# dependencies
/node_modules
/.pnp
/prisma
.pnp.js

# misc
.DS_Store
*.pem
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env
.env*.local
.env.production

# typescript
*.tsbuildinfo

# anchor / solana
/target
/programs/**/target
.anchor
.surfpool
**/*.rs.bk

# indexer (a member of the root Cargo workspace — see /Cargo.toml — so its
# build output lands in the shared /target above, not its own; dlmm-bridge
# is a plain Node package, unaffected by that)
/indexer/dlmm-bridge/node_modules
/indexer/dlmm-bridge/dist

# git worktrees
/.worktrees

# impeccable design skill — local live-mode session state and hook cache,
# not project config (that's just /.impeccable/live/config.json, tracked)
/.impeccable/live/sessions
/.impeccable/live/accept-receipts
/.impeccable/hook.cache.json
