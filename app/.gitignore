# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env
.env*.local
.env.production

# database (sqlite dev db is generated)
/prisma/dev.db
/prisma/dev.db-journal
*.db
*.db-journal

# typescript
*.tsbuildinfo

# testing
/coverage

# vercel
.vercel

# NEB launch config — real values (keypair paths, mainnet mints) shouldn't
# be committed; scripts/launch-neb/launch-neb.config.example.jsonc is the
# tracked template.
/scripts/launch-neb/launch-neb.config.json

# Deploy script config — real values (keypair paths, Render service ids)
# shouldn't be committed; scripts/deploy/deploy.config.example.jsonc is the
# tracked template.
/scripts/deploy/deploy.config.json
